namespace EjemploLexer
{
    public enum TokenTypes
    {
        EOF,
        ID,
        INT_TYPE,
        BOOLEAN_TYPE,
        VOID_TYPE,
        REAL_TYPE,
        STRING_TYPE,
        DATE_TYPE,
        STATIC_TYPE,
        IF,
        WHILE,
        ELSE,
        RETURN,
        INICIO,
        IF_THEN,
        DO,
        END,
        ADD_OP,
        SUB_OP,
        MULT_OP,
        DIV_OP,
        ASIG,
        TRUE_LITERAL,
        FALSE_LITERAL,
        PARENTESIS_OPEN,
        PARENTESIS_CLOSE,
        LLAVE_OPEN,
        LLAVE_CLOSE,
        Digit,
        FN_STM


    }
}